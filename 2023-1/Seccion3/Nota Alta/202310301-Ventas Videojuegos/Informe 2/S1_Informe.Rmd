---
title: "Ventas Videojuegos"
date: "1 de Junio del 2023"
output:
  html_document:
    df_print: paged
  pdf_document: default
  word_document: default
---

# Índice:

<ol>
  <li>
    <a href="#introducción">Introducción</a>
    <ol>
      <li><a href="#tema">Tema</a></li>
      <li><a href="#relevancia">Relevancia</a></li>
      <li>
        <a href="#objetivos">Objetivos</a>
        <ol>
          <li><a href="#objetivos-generales">Objetivos Generales</a></li>
          <li><a href="#objetivos-específicos">Objetivos Específicos</a></li>
        </ol>
      </li>
      <li><a href="#contexto">Contexto</a></li>
    </ol>
  </li>
  <li>
    <a href="#datos">Datos</a>
    <ol>
      <li><a href="#recolección-de-datos">Recolección de datos</a></li>
      <li><a href="#población-muestra-y-muestreo">Población, muestra y muestreo</a></li>
      <li><a href="#variables">Variables</a></li>
    </ol>
  </li>
  <li><a href="#análisis-descriptivo">Análisis Descriptivo</a></li>
  <li>
    <a href="#probabilidades">Probabilidades</a>
    <ol>
      <li><a href="#probabilidad-empírica">Probabilidad empírica</a></li>
      <li><a href="#probabilidad-condicional">Probabilidad condicional</a></li>
    </ol>
  </li>
  <li>
    <a href="#variables">Variables</a>
    <ol>
      <li><a href="#variables-discretas">Variables discretas</a></li>
      <li><a href="#variables-continuas">Variables continuas</a></li>
    </ol>
  </li>
</ol>


## 1. Introducción

### 1.1 Tema

Análisis de las ventas en la industria de videojuegos comprendidos entre los años 2015 a 2016 mediante una base de datos.

### 1.2 Relevancia

Como en  la industria de videojuegos en la actualidad  genera casi  lo mismo o más que la industria de cine  y la música actualmente , por ello hemos visto convenientes que las inversora de la industrias de videojuegos puedan saber  a qué sector invertir  para obtener las mayoras ganancias posibles en las diversa regiones de Europa ,América, países como Japón entre otros con el fin de que estas pueden tener gustos diferentes o similares , también de conocer los riesgos en estos mercados y saber el con que desarrolladora y distribuidora contar. Además de tener en cuenta las críticas de los videojuegos pública y profesional, la calificaciones de edad y  los tipos de juegos mas demandado .Por ello, hemos analizado  los juegos lanzados de 2015 a 2016 para tener una idea de como fue el mercado durante este tiempo para conocer los éxitos y fracasos que hubo.

### 1.3 Objetivos

-   Es importante tener un objetivo claro antes de comenzar el análisis para garantizar que el análisis sea relevante y útil.

#### 1.3.1 Objetivos Generales

-   Determinar cuáles fueron las categorías de los juegos más vendidos además como influye en el desarrollo de la industria de los videojuegos.
-   Comparar las ventas de videojuegos en diferentes regiones geográficas y determinar las diferencias en los patrones de compra de los consumidores. Predecir las ventas futuras de videojuegos en la industria utilizando modelos estadísticos y de aprendizaje automático.

#### 1.3.2 Objetivos Específicos

-   Examinar los videojuegos por país (Europa,Norteamérica y Japón )
-   Analizar cuáles fueron los distribuidores de videojuegos con más publicaciones.
-   Precisar que desarrolladora de videojuegos fue la más demandante.
-   Identificar patrones y tendencias de venta en la industria de los videojuegos entre 2015 y 2016, incluyendo los géneros de videojuegos más populares y las plataformas de juegos más utilizadas.
-   Analisar que plataformas de videojuegos dominaron mas en el año 2015 y 2016, a su vez en que año dichas plataformas tuvieron mas demandas.
-   Tendencias de clasificaciones de edad por género de videojuego.

### 1.4 Contexto

El análisis de las ventas en la industria de videojuegos entre los años 2015 y 2016 es de gran relevancia para comprender el desempeño y las tendencias del mercado en este sector en constante evolución. A través de una base de datos específica, se busca examinar y entender las ventas en esta industria durante ese período.

La industria de los videojuegos ha experimentado un crecimiento significativo en las últimas décadas, convirtiéndose en una forma de entretenimiento popular y altamente lucrativa a nivel mundial. El análisis de las ventas en este sector proporciona información crucial sobre los patrones de consumo, las preferencias de los jugadores y el éxito de los diferentes títulos y géneros de videojuegos.

Con el análisis de las ventas de videojuegos entre 2015 y 2016, se pretende identificar patrones de ventas, explorar relaciones entre variables relevantes y examinar las tendencias de consumo de videojuegos durante ese período específico.


## 2. Datos

La base de datos proporcionada contiene información detallada, como el nombre del videojuego, la plataforma, el año de lanzamiento, el género, el editor, las ventas en diferentes regiones, las ventas globales, las puntuaciones de los críticos y usuarios, la cantidad de críticas y usuarios, el desarrollador y la clasificación por edad recomendada ("Rating").   

### 2.1 Recolección de datos

La página web de donde fue sacada la información de datos fue la siguiente: **"kaggle.com"**

Para nuestros instrumentos de recolección nos basamos en los siguientes criterios:

- La reputación de la fuente: La fuente es conocida por ser confiable y precisa, es por ello que lo más probable es que sus datos sean confiables. Ademas de que fue recomendada por el profesor.

- Esta base de datos tiene información acerca de los juegos descargados desde los años 1980 hasta el 2016, nosotros resumimos la base de datos para analizarlo desde el 2015 hasta 2016 esto con el fin de tener datos más actualizados.

Para ser precisos la base de datos descargado lo encontramos en el siguiente link:: "<https://www.kaggle.com/datasets/gregorut/videogamesales>"


### 2.2 Población, muestra y muestreo

Población:

La población es 16 598 registros de videojuegos con la condición de que tengan en común lograr superar las 100 000 copias  

Muestra y muestreo:

La muestra que hemos elegido son los juegos de 2015 a 2016 que contiene 455 videojuegos que superaron la 100 000 copias
El tipo de muestra es No probabilístico, debido a que contamos con una gran cantidad de datos para el año 2015 y a 2016 , con toda su información completa de 16 tipos de variables .



### 2.3 Variables

Para realizar el estudio hemos contado con las siguientes variables.

| Nombre de variable |   Tipo de variable    | Descripción de variable                                                                                                                                                                                          |
|----------------------:|:-------------------:|:---------------------------|
|               Name |  Cualitativa nominal  | Esta variable contiene el nombre de los videojuegos del estudio.                                                                                                                                                 |
|           Platform |  Cualitativa nominal  | Esta variable contiene la plataforma donde fueron lanzados los videojuegos.                                                                                                                                      |
|    year_of_realese | Cuantitativa continua | Esta variable contiene el año de lanzamiento de los videojuegos.                                                                                                                                                 |
|              Genre |  Cualitativa nominal  | Esta variable contiene el género de los videojuegos.                                                                                                                                                             |
|          Publisher |  Cualitativa nominal  | Esta variable contiene la entidad que publicó el videojuego.                                                                                                                                                     |
|           NA_sales | Cuantitativa continua | Esta variable contiene el número de ventas de los videojuegos en Norteamérica.(En millones)                                                                                                                      |
|           EU_sales | Cuantitativa continua | Esta variable contiene el número de ventas de los videojuegos en Europa.(En millones)                                                                                                                            |
|           JP_sales | Cuantitativa continua | Esta variable contiene el número de ventas de los videojuegos en Japón.(En millones)                                                                                                                             |
|        Other_sales | Cuantitativa continua | Esta variable contiene el número de ventas de los videojuegos en el resto del mundo.(En millones)                                                                                                                |
|       Global_sales | Cuantitativa continua | Esta variable contiene el número de ventas totales de los videojuegos en todo el mundo.(En millones)                                                                                                             |
|       Critic_score | Cuantitativa continua | Esta variable contiene el número de la calificación de juego según críticos profesionales del 1 al 100 de puntuación.                                                                                            |
|       Critic_count | Cuantitativa continua | Esta variable contiene el número de los profesionales que realizaron la crítica(En unidad)                                                                                                                       |
|         User_score | Cuantitativa continua | Esta variable contiene el número de la calificación por el público que realizaron la crítica, la puntuación el del 1 al 100 de puntuación.                                                                       |
|         User_count | Cuantitativa continua | Esta variable contiene el número del público que realizaron la crítica.                                                                                                                                          |
|          Developer |  Cualitativa nominal  | Esta variable contiene la entidad que desarrolló el videojuego.                                                                                                                                                  |
|             Rating |  Cualitativa ordinal  | Esta variable contiene la clasificación por categoría de edad para el juego,("E" para todas las edades, "E10" de 10 o más,"T"para adolescentes o más, "M" para 17 o más,"A"solo para mayores de 18). |

### 2.4 Limpieza de Datos


- Hemos utilizado la función ***deleate.na*** para eliminar las filas que no tengan data.


```{r}
library(dplyr)
getwd()
juegos=read.csv("juegos.csv")


delete.na <- function(df, n=0) {  #ELIMINAR LAS FILAS QUE NO TENGAN DATA 
 df[rowSums(is.na(df)) <= n,]   
  
}

```

```{r}
juegos_sin_0 = delete.na(juegos)   

```

- Como tenemos datos muy antiguos nosotros nos hemos especificado entre los años 2015-2016.

- Luego asignamos estos nuevos datos a la nueva variable [***juegos2015_2016***](google.com).

```{r}
juegos2015_2016=juegos_sin_0[juegos_sin_0$Year_of_Release >= 2015 & juegos_sin_0$Year_of_Release <= 2017 , ] ##quedarme solo con los años 2015 a 106
juegos2015_2016 <- subset(juegos2015_2016, !Rating == "" )
juegos2015_2016


```

```{r}
#Algunas variables no estaban clasificadas correctamente, debido a esto, se tuvo que cambiarlas al tipo de variable que pertenecían.
# Convertir las columnas de ventas a tipo numérico
juegos2015_2016$NA_Sales <- as.numeric(juegos2015_2016$NA_Sales)
juegos2015_2016$EU_Sales <- as.numeric(juegos2015_2016$EU_Sales)
juegos2015_2016$JP_Sales <- as.numeric(juegos2015_2016$JP_Sales)
juegos2015_2016$Other_Sales <- as.numeric(juegos2015_2016$Other_Sales)
```

## 3. Análisis descriptivo

### Comparar las ventas en diferentes regiones con el género de videojuego: *North America*
```{r}
# Filtrar solo para la región de América del Norte
ventas_NA <- juegos2015_2016[, c("Genre", "NA_Sales")]

# Sumar las ventas por género
ventas_NA <- aggregate(NA_Sales ~ Genre, data = ventas_NA, FUN = sum)

# Ordenar los géneros por ventas en América del Norte
ventas_NA <- ventas_NA[order(ventas_NA$NA_Sales, decreasing = TRUE), ]


# Añadir etiquetas
text(x = barplot(ventas_NA$NA_Sales,
                 col = "blue",
                 ylim = c(0, max(ventas_NA$NA_Sales) * 1.1),
                 axes = FALSE,
                 main = "Ventas en América del Norte por Género de Videojuego",
                 xlab = "Género",
                 ylab = "Ventas en América del Norte"),
     y = ventas_NA$NA_Sales,
     labels = ventas_NA$NA_Sales,
     pos = 3,
     col = "black")

# Añadir una línea de referencia para la media de ventas
abline(h = mean(ventas_NA$NA_Sales),
       col = "red",
       lwd = 2,
       lty = 2)

# Agregar una leyenda
legend("topright",
       legend = c("Ventas en América del Norte", "Media de Ventas"),
       fill = c("blue", "red"),
       title = "Información",
       cex = 0.8)
```

Lo que podemos concluir es que los géneros de videojuegos que dominan en América del Norte , son primero **"shooters"**(Juegos de disparos), segundo juegos de **"Acción"**, tercero juegos de **"Deportes"** y cuarto juegos de **"rol"** ,según los juegos que superan la línea de media.

Estos datos son muy útiles ya que las empresa sabrán en qué tipo de juegos valen la pena invertir grandes cantidades con el objetivo de obtener ganancias en América del Norte.





### Tabla de ventas totales en cada región 
```{r}
# Agrupar y sumar las ventas por región
ventas_totales <- aggregate(cbind(NA_Sales, EU_Sales, JP_Sales, Other_Sales) ~ 1, data = juegos2015_2016, FUN = sum)
colnames(ventas_totales) <- c("Ventas totales en Norte America", "Ventas totales en Europa", "Ventas totales en Japon", "Ventas totales en Otras regiones")

# Ordenar los totales de ventas de mayor a menor
ventas_totales <- ventas_totales[order(ventas_totales$`Ventas totales en Norte America` + ventas_totales$`Ventas totales en Europa` + ventas_totales$`Ventas totales en Japon` + ventas_totales$`Ventas totales en Otras regiones`, decreasing = TRUE), ]

# Tabla de totales de ventas
ventas_totales
```

Algunas formas en las que esta tabla podría ser útil en el estudio incluyen:
.Comparación de ventas por región
.Identificación de patrones por región
.Tabla de ventas por región, lo que puede ayudar a comprender mejor el mercado y tomar decisiones informadas en tu estudio o análisis relacionado con los videojuegos.




### Identificar patrones y tendencias de venta en la industria de los videojuegos entre 2015 y 2016, incluyendo los géneros de videojuegos más populares y las plataformas de juegos más utilizadas.

```{r}
#Tendencia de las Plataformas más utilizadas


cantidad_plat <- table(juegos2015_2016$Platform) #Cantidad de plataformas utilizadas en los videojuegos

barplot(cantidad_plat,col="burlywood",main = "Plataformas de videojuegos más utlizadas entre 2015 y 2016",xlab = "Plataforma",ylab ="frecuencia")

legend("topright",
       legend = c("Plataformas más utilizadas","Media de las plataformas"),
       fill = c("burlywood","red"),
       title = "Información",
       cex = 0.8)

abline(h = mean(cantidad_plat),
       col = "red",
       lwd = 2,
       lty = 2)
```
análisis:
. útil para desarrolladoras de videojuegos, para saber a qué plataforma conviene adaptar su producto.
. No conviene adaptar en la Ps3
. Conviene adaptar juegos a Ps4, XOne y PC


```{r}
#Tendencia de los generos más populares

cantidad_gen <- table(juegos2015_2016$Genre)
barplot(cantidad_gen,col="darkolivegreen2",main = "Popularidad de videojuegos por género entre 2015 y 2016",xlab = "Genero",ylab ="Preferencia")

legend("topright",
       legend = c("Popularidad de videojuegos","Media de la popularidad"),
       fill = c("darkolivegreen2","red"),
       title = "Información",
       cex = 0.8)

abline(h = mean(cantidad_plat),
       col = "red",
       lwd = 2,
       lty = 2)
```

En esta gráfica nos muestra en general se puede concluir que la popularidad es más en el genéro **Action** y menos en **Puzzle**.  .

```{r}

#Comparacion en que año las plataformas de videojuegos tuvieron mas demanda.
Tabla_Comparacion_2 = prop.table(table(juegos2015_2016$Year_of_Release,juegos2015_2016$Platform), margin = 2)*100

mosaicplot(Tabla_Comparacion_2 ,main = "Tabla de Comparación",xlab = "Años",ylab = "Juegos",color = c("green","blue","red","yellow","black","pink","orange"))


```



Resumen del análisis.

1. Los juegos que dominaron mas en el año 2015 y 2016 fueron la PC, el PS4 y el XOne.
2. Algunas plataformas que disminuyeron su demanda para el año 2016 fueron: 3DS, PSV y WiiU, y las otras plataformas como: PC, PS4 y XOne aumentaron.  
3. Finalmente las PS3  bajo su demanda drasticamente para el año 2016.

### Puntuación promedio por cada Género

```{r}
#Separamos la data que usaremos
genero <- juegos2015_2016[, c("Genre", "Critic_Score")]
#Promedio para cada genero
genero_promediado <- aggregate(Critic_Score ~ Genre, data = genero, FUN = mean)

genero_promediado <- genero_promediado[order(genero_promediado$Critic_Score, decreasing = TRUE), ]
#Redondeamos
genero_promediado$Critic_Score <- round(genero_promediado$Critic_Score,2)
#Graficamos 

text(x = barplot(genero_promediado$Critic_Score,
                 
                 col = "light green",
                 ylim = c(0, max(genero_promediado$Critic_Score) * 1.3),
                 axes = FALSE,
                 main = "Puntuacion promedio de cada genero",
                 xlab = "Género",
                 ylab = "Puntuacion Promedio"),
     y = genero_promediado$Critic_Score,
     labels = genero_promediado$Critic_Score,
     pos = 3,
     col = "black")
legend("topright",
       legend = c("Puntuacion promedio por género"),
       fill = c("light green"),
       cex = 0.6)
```

Al realizar este análisis podemos ver en la gráfica no hay mucha variación en la puntuación, pero también podemos deducir cuales son los 3 primeros géneros que llaman más la atención de la población, por ende podemos aumentar el número de videojuego en el mercado variando el tipo entre estos tres géneros.También con esta información les servirá a los creadores a mejorar la calidad de los videojuegos.


### Tendencias de clasificaciones de edad por género de videojuego

```{r}
library(ggplot2)
ggplot(juegos2015_2016, aes(x = Rating, fill = Genre)) +
  geom_bar() +
  labs(x = "Clasificación de edad", y = "Frecuencia", title = "Distribución de clasificaciones de edad por género de videojuego")
```


Este tipo de gráfico  brinda una visión general de cómo se distribuyen las clasificaciones de edad en cada género de videojuegos. Se puede observar las diferencias relativas entre los géneros y determinar si hay géneros específicos que tienden a tener ciertas clasificaciones de edad con más frecuencia.

## 4. Probabilidades

### 4.1 Probabilidad empírica
 

ejemplo 1
1) Experimento: Seleciona un juego y observa  de que plataforma pertenece
2)Experimento resultado
```{r}
table(juegos2015_2016$Platform)
```

u =[3ds, pc, ps3.ps4,psv,wiiu yxbox one]
3) Identificar evento
A1 : Selecionar un juego que sea 3ds
A2: Selecionar un juego que sea PS3
A3: Selecionar un juego que sea PS4
A4: Selecionar un juego que sea PSV
A5: Selecionar un juego que sea WIIU
A6: Selecionar un juego que sea XONE

4)Probabilidades 
Hallamos las probabilidades de cada evento representado en una grafica y tabla

```{r}

# Calcular las probabilidades empíricas para la variable "Genre"
probabilidades_genre <- prop.table(table(juegos2015_2016$Platform))
generos <- names(probabilidades_genre)

# gráfico de barras para las probabilidades empíricas de "Genre"
barplot(probabilidades_genre, main = "Probabilidades empíricas de género de videojuegos", xlab = "Platform", ylab = "Probabilidad", col = rainbow(length(probabilidades_genre)), names.arg = generos)

# Leyenda 
legend("topright", legend = paste(generos, "(", round(probabilidades_genre * 100, 2), "%)"), fill = rainbow(length(probabilidades_genre)), cex = 0.8)


```

Experimento de plataforma de vidiojuego
omega[3ds pc ps3 ps4 psv wiiu xones]


En el contexto del análisis de géneros de videojuegos, si un género tiene una probabilidad empírica más alta, indica que ese género aparece con mayor frecuencia en la base de datos en comparación con otros géneros.

Por ejemplo, si el género "Acción" tiene una probabilidad empírica del 32.53% y el género "Aventura" tiene una probabilidad empírica del 5.71%, significa que los videojuegos del género "Acción" son más comunes en la muestra que los videojuegos del género "Aventura".

La probabilidad empírica es una medida descriptiva que permite observar las tendencias y patrones de frecuencia en los datos. Al comparar las probabilidades empíricas de diferentes géneros, se pueden identificar los géneros más prevalentes en la muestra y comprender la distribución relativa de los géneros de videojuegos en el período analizado.

ejemplo 2 empirica

1) Experimento: Seleciona un juego y observa  de que genero de videojuego que  pertenece
2)Experimento resultado
```{r}
table(juegos2015_2016$Genre)
```

u =[ Action, Adventure     , Fighting ,Misc, Platform  ,   Puzzle   , Racing ,Role-Playing  ,  Shooter ,Simulation, Sports     Strategy  ]
3) Identificar evento
A1 : Selecionar un juego que sea Action
A2: Selecionar un juego que sea Adventure
A3: Selecionar un juego que sea Fighting
A4: Selecionar un juego que sea Misc
A5: Selecionar un juego que sea Platform
A6: Selecionar un juego que sea Puzzle
A7: Selecionar un juego que sea Racing
A8: Selecionar un juego que sea Role-Playing
A9: Selecionar un juego que sea Shooter
A10: Selecionar un juego que sea Simulation
A11: Selecionar un juego que sea Sports
A12: Selecionar un juego que sea Strategy

4)Probabilidades 
Hallamos las probabilidades de cada evento representado en una grafica y tabla

```{r}

# Calcular las probabilidades empíricas para la variable "Genre"
probabilidades_genre <- prop.table(table(juegos2015_2016$Genre))
generos <- names(probabilidades_genre)

#  gráfico para las probabilidades empíricas de "Genre"
barplot(probabilidades_genre, main = "Probabilidades empíricas de género de videojuegos", xlab = "genero experimento", ylab = "Probabilidad", col = rainbow(length(probabilidades_genre)), names.arg = generos)

# Leyenda
legend("topright", legend = paste(generos, "(", round(probabilidades_genre * 100, 2), "%)"), fill = rainbow(length(probabilidades_genre)), cex = 0.8)
```
variable:
selecionar en el Experimento la data de videojuego estudiar y observar el dominio de genero videojuego

Resultado omega{Probabilidades}: cual es probalidad de elegir un videojuego de accion 32.53

En el contexto del análisis de plataformas de videojuegos, si una plataforma tiene una probabilidad empírica más alta, indica que esa plataforma aparece con mayor frecuencia en la base de datos en comparación con otras plataformas.

Por ejemplo, si la plataforma "PS4" tiene una probabilidad empírica de 40,44% y la plataforma "XOne" tiene una probabilidad empírica del 24.62%, significa que los videojuegos de la plataforma "PlayStation4" son más comunes en la muestra que los videojuegos de la plataforma "Xbox".


### 4.2 Probabilidad condicional

Ejemplo 1


1. Definimos nuestros eventos:

$A: \text{está definido como seleccionar un videojuego y que su crítica por un profesional sea mayor a 50}$
$B: \text{está definido como seleccionar un videojuego y que su crítica por los usuarios sea menor de 5 }$
 

2. Validando la independencia:

- Para el evento A: 
```{r}
#Cantidad de videojuegos
t_videojuegos = nrow(juegos2015_2016) 

# Cantidad de juegos que cumplen la condición

rep_SumA = sum(juegos2015_2016$Critic_Score > 50)

# Calculamos la probabilidad
prob_A= rep_SumA/ t_videojuegos

# Mostrar la probabilidad
prob_A


```
El complemento de $A ' = 1 - p(A)$


- Para el evento B: 
```{r}
table(juegos2015_2016$User_Score)

# Contamos la cantidad de juegos que cumplen la condición que sean menor o igual que 5

rep_SumB = sum(juegos2015_2016$User_Score <= 5)

#Calculamos la probabilidad

prob_B = rep_SumB / t_videojuegos

prob_B

```
El complemento de $B ' = 1 - p(B)$

- Comprobando la independecia de cada evento 

$P(A ∩ B) = P(A). P(B)$

```{r}
#Intersección

Evento_A_Interseccion_B <- sum (juegos2015_2016$Critic_Score > 50 & juegos2015_2016$User_Score <= 5)
Pro_Evento_A_Interseccion_B <- Evento_A_Interseccion_B/t_videojuegos
Pro_Evento_A_Interseccion_B

#P(A).P(B)
prob_A*prob_B

```
Vemos que sale diferente por ende ambas son variables independientes también lo podemos deducir analiticamente ya que la crítica por parte de los usuarios es un evento independiente a la critica por parte de los usuarios, al modificar algun parametro este no se vería adectado.


3. Teorema de Bayes:

$P(A|B) = P(A∩B) / P(B)$

```{r}

#prob_A
#prob_B

Pro_Evento_A_si_Evento_B <- Pro_Evento_A_Interseccion_B/prob_B
Pro_Evento_A_si_Evento_B
```

$P(B|A) = P(A|B).P(B)/P(A)$

```{r}
Pro_Evento_B_si_Evento_A <- (Pro_Evento_A_Interseccion_B * prob_B)/prob_A

Pro_Evento_B_si_Evento_A 

```




Ejemplo 2

Eventos del experimento de la vase de datos de juegos:
EVENTO A: Selecionar un juego con venta 0.01 millones norte america  (dependiente) 
Evento B: Selecionar un juego que sea de ps4 (independiente)
1)vaidad independencia
.P(A ∩ B)=P(A)*P(B)  .... a11/T = TA/T *TB/T
.P(A|B)=P(A)*(a11/TB=TA/T)
```{r}

tabla_frecuencia =  table(juegos2015_2016$Platform,juegos2015_2016$NA_Sales)
#P(A ∩ B)=P(A)*P(B)  .... a11/T = TA/T *TB/T
#16/66 =38/66 *29/66
38/66 *29/66  #SI CUMPLE
#.P(A|B)=P(A)*(a11/TB=TA/T)
#16/29 =38/66  # MARGEN DE ERROR DE 0.001%
38/66
```
3)TEOREMA DE BAYES, cual es la probabilidad para que la venta de ps4 se hasta 0.01
P(B|A)= (P(A|B)P(B))/P(A)
```{r}
#PA=a11/tb *tb/t +a11/tb*tb/t
PA=(16/29)*(29/66)+(22/28)*(28/66)
#(P(A|B)P(B)):
P= (38/66)*(29/66)
#PA=a11/tb *tb/t +a11/tb*tb/t:
PA= P/PA
PA # el la p probabilidad de que el juego sea pese cuatro con venta de 0.01
```
Pudimos comprobar la formulas y las condiciones de probabilidades condiconales


## 5. Variables


### 5.1 Variables discretas
#Variables discretas plataforma 
1)  Definimos que distribución se usara 
 Aplicare tristibucion Binomial
 Mi variable es la plataforma de videojeugo, su exito es si la plataforma de videojuego es ps4, P(Exito) = f(#Ps4)/n,
```{r}
n=length(juegos2015_2016$Platform)# el numero de elementos en en plataforma

tabla_plataforma=table(juegos2015_2016$Platform)
ps4=tabla_plataforma[4]
probabilidad_de_exito = ps4/n # wa
probabilidad_de_exito # es la probabilidad que el juego sea de play 4

```

 
 
2) contruir variables aleatoria
X :# numero de juego que  pertenecen a la consola ps4 en el conjuto 'n'

3)Identificar parametros

X ~ Bin(p,n),      {p =P(E) ,la proabilida de exito, de ser de la plataforma ps4},     {n = 455 tamaño de la muestra}

```{r}
# el numero de elementos en en plataforma
tabla_plataforma=table(juegos2015_2016$Platform[0:50])
ps4=tabla_plataforma[2]
probabilidad_de_exito = ps4/n # wa
print("probabilidad de exito para las consolas de ps4")
probabilidad_de_exito # es la probpabilidad que el juego sea de play 4

print("El numero de elementos es:")
print(n)

```

4)Construcion de densidad
```{r}
n=427
p=probabilidad_de_exito


plot(dbinom(0:n,n,p),xlab ="numero de juego que perteneces a la ps4",ylab = "probabilidad del juego este en la consola ps4" )

```
5) validad propiedades
```{r}

#segunda version

# Definir los valores de la variable aleatoria y sus probabilidades correspondientes
valores = c(0:n) #tamaño de la muestra
proba_ps4 = dbinom(0:n,n,probabilidad_de_exito) #Probabilidad para cada persona

# Calculamos la esperanza , varianza , desviación estánda y coeficiente de variación
esperanza = sum(valores * proba_ps4)

varianza = sum(proba_ps4 * (valores - esperanza)^2)

desviacion_estandar = sqrt(varianza)

coeficiente_variacion = (desviacion_estandar / esperanza) * 100


esperanza
varianza
coeficiente_variacion



```


#carlos


#### Variable discreta Género 

1. Utilizaremos la variable Género y la asociaremos a la distribución binomial

2. Definimos nuestra variable aleatoria:
  **X**: es el número de videojuegos que pertenezcan a la categoría de **Shooter** en un conjunto "n" 

3. Identificamos los parámetros:

  $X \sim \text{bin}(p,n)$;

  Siendo:

  $p = P(E) : \text{la probabilidad de éxito que está definido como seleccionar un videojuego que sea de la categoría Shooter}$
  $n=50 : \text{está definido como el tamaño de la muestra}$

4. Construimos la gráfica densidad:
 - Hallamos la probabilidad de éxito

```{r}
n=50 #tamaño de la muestra (verificar)
table_shooter = table(juegos2015_2016$Genre[0:n]) #identificamos Shooter 
shooter = table_shooter[8]

p_exito_shooter = shooter / n
p_exito_shooter 
```
 - Ahora que sabemos que la probabilidad es de **0.32**, procedemos a contruir la gráfica:
 
```{r}

plot(dbinom(0:n,n,p_exito_shooter),xlab = "Número de videojuegos que pertenecen al género Shooter", ylab = "Probabilidad de videojuego que sea Shooter",col="red")
```

5. Validando las propiedades de la variable aleatoria

```{r}
# Definir los valores de la variable aleatoria y sus probabilidades correspondientes
valores = c(0:50) #tamaño de la muestra
proba_shooter = dbinom(0:n,n,p_exito_shooter) #Probabilidad para cada persona

#Calculamos la esperanza , varianza , desviación estánda y coeficiente de variación

esperanza = sum(valores * proba_shooter)

varianza = sum(proba_shooter * (valores - esperanza)^2)

desviacion_estandar = sqrt(varianza)

coeficiente_variacion = (desviacion_estandar / esperanza) * 100


esperanza
varianza
coeficiente_variacion


```
 6. Construyendo un ejemplo:
 
Electronic Entertainment Expo (E3) es la feria de videojuegos más importante del mundo y uno de los eventos de gaming más importantes, se desea conocer la probabilidad de que más del 50% de los asistentes elijan un videojuego y sea del género Shooter sabiendo que en esa feria asistieron 50 personas.

```{r}
# Probabilidad de éxito (probabilidad de que una persona elija un videojuego Shooter)
probabilidad_exito <- 0.32

# Número de asistentes a la E3
asistentes <- 50

# Calculamos la probabilidad utilizando la distribución binomial acumulativa
probabilidad <- 1 - pbinom(asistentes * 0.5, asistentes, probabilidad_exito)

#Resultado
print(probabilidad)


```



#VARIABLES CONTINUAS 
###EJEMPLO 1 VARIABLE CONTINUA

### Claificación de juegos segun criticos.

```{r}
#Nuestra varible continua es "Clasificación de juegos segun criticos"
#Extraemos Critic_Score.
#Tabla de para nuestra variable Critic_Score.
Critic <- table(juegos2015_2016$Critic_Score)
Critic

#Graficamos, nuestro grafico de densidad de la tabla para saber a que modelo de distribución Probabilistico se asemeja mas.
plot(Critic, xlab = "Puntuacion", ylab = "Numero de juegos", col = c("blue"))

```




La siguiente grafica nos muestra como es el comportamiento de la variable Critic_Score que parece ser una campana; como vemos se asemeja mucho a un modelo de distribución Normal.


```{r}

#Tabla de probabilidad de la variable Critic_Socore.
Critic_Pro <- prop.table(Critic)


#Graficamos para confirmar que se asemeja a una Distribución Normal. 
plot(Critic_Pro, xlab = "Puntuación", ylab = "Probabildad de cada juego", col = c("purple"))


```


Confirmamos que se asemeja mucho a una Distribución Normal.

Ahora pasamos a validar si en verdad se comporta como una Distribución Normal.

```{r}

#Para nuestra primera comprobación:
#Se debe cumplir que la media y la mediana deben aproximarse a un valor cercano.

#Creamos nuestra función para la media:

Calcular_Media <- function(Dato){
  total <- length(Dato)
  media <- sum(Dato)/total
  return(media)
}

Media = Calcular_Media(Critic_Pro)


# Función para mediana:

Calcular_Mediana <- function(Dato){
  total <- length(Dato)
  ordenados <- sort(Dato)
  
  if (total %% 2 == 0) {
    indice1 <- total / 2
    indice2 <- indice1 + 1
    mediana <- (ordenados[indice1] + ordenados[indice2]) / 2
  } else {
    indice <- (total + 1) / 2
    mediana <- ordenados[indice]
  }
  
  return(mediana)
}

Mediana = Calcular_Mediana(Critic_Pro)
Mediana

Media


#Con comandos:
mean(Critic_Pro)
median(Critic_Pro)

#Tenemos los siguientes resultamos:
#Media: 0.01694915
#Mediana: 0.01098901

#Se aproximan a un valor en comun que es: 0.01
#Paso la primera prueba con exito.

```

```{r}
#Segunda comprobación:
#Se debe cumplir lo que es las propiedades de la variaza y la desviación estandar al cuadrado, estas deben aproximarse a un valor cercano.

#Creamos nuestra funcion de la Varianza:

Calcular_Varianza <- function(Dato) {
  total <- length(Dato)  # elementos
  media <- mean(Dato)  #  media
  suma_cuadrados <- sum((Dato - media)^2)  # Suma de los cuadrados de las desviaciones
  varianza <- suma_cuadrados / (total - 1)  # Cálculo de la varianza (usando n-1 para el denominador)
  return(varianza)
}

Varianza = Calcular_Varianza(Critic_Pro)
Varianza

#Varianza mediante el comando var():
var(Critic_Pro)

Des_Est = sd(Critic_Pro)^2
Des_Est

#Tenemos los siguientes resultamos:
#Varianza: 0.0001953872
#Desviación Estandar al Cuadrado: 0.0001953872

#Nos damos cuenta que sus valores son iguales: 0.0001953872
#Por lo tanto decimos que la segunda prueba que tratava de sus propiedades paso con exito.

```

```{r}
#Para nuestra Tercera comprobación:
#Se debe cumplir que la esperanza y la media deben aproximarse a un valor cercano.

#función Esperanza:

Calcular_Esperanza <- function(Dato, Prob){
  esperanza<- sum(Dato * Prob)
  return(esperanza)
}

#Base de datos
Critic_ <- juegos2015_2016$Critic_Score

#Prob, Critic_Pro
Critic_Pro_ <- prop.table(Critic_)


Esperanza = Calcular_Esperanza(Critic_, Critic_Pro_)
Esperanza
mean(Critic_)


#Tenemos los siguientes resultamos:
#Esperanza: 74.9656
#Media :72.96923

#Nos damos cuenta que sus valores se aproximan casi muy cercano a un valor.
#Por lo tanto decimos que la tercera prueba paso con exito.

```

#Con los resultamos que tenemos, podemos decir que se aproximan a una Distribución Normal
#Por lo tanto nuestros parametros seran:

#Damos valores a nuestro mu y la desviacion estandar.

```{r}
#Parametro:

mu = mean(Critic_)
mu
desviacion_estandar = sd(Critic_)
desviacion_estandar

#Por lo tanto decimos:

#Variable Critic_Score ~ N (72.96923, 12.08282)

#¿Cuál es la probabilidad de que un critico de los videojuegos ponga un puntaje más de 80 puntos?

#Como vemos tenemos 2 maneras de hallarlo:
#1
pnorm(80,72.97,12.08, lower.tail = F)
#2
1 - pnorm(80,72.97,12.08)


#Respuesta: La probabilidad de obtener un puntaje más de 80 puntos es de 0.280299.

#¿Cuál es la probabilidad de que un critico de los videojuegos ponga un puntanje entre 30 y 60 puntos?

pnorm(60,72.97,12.08) - pnorm(30,72.97,12.08)

#Respondiendo a la pregunta: La probabilidad de obtener un puntaje entre el 30 y 60 puntos es de :0.1412966

```






###EJEMPLO 2 VARIABLE CONTINUA
### Ventas Globales de Los Juegos 

```{r}
# Filtrar ventas globales 
v_global <- juegos2015_2016[, c("Global_Sales")]
venta_g <- v_global[4:length(v_global)]
#Graficar 
plot(v_global, xlab = "Posicion del videojuego", ylab = "Ventas Globales(millon)", col = c("#00CDCD"))

#Obtenemos la venta máxima y cual fue el juego
max(v_global)
juegos2015_2016$Name[which.max(max(v_global))]
```
Aquí seleccionamos la data de solo las ventas globales, luego realizamos una gráfica para poder ver el comportamiento de la variable, el cual se puede visualizar como una variable continua que tiene una aproximación a una distribución exponencial.
Analizando la gráfica podemos ver que el videojuego más vendido es el FIFA 16 con una venta global de 8.57 millones.

Posteriormente hacemos una validación del modelo, lo cual por teoría sabemos que la media y la desviación estándar deben ser cercanas.

```{r}
media <- mean(venta_g)
desviacion <- sd(venta_g)
varianza <- var(venta_g)
esperanza <- mean(venta_g)

media
desviacion
varianza

```
Hallando la media y desviación estándar podemos ver que hay una pequeña diferencia entre ellos, esto es debido a que hay algunos datos de la variable que están fuera de la distribución exponencial, eso hace que haya algunas variaciones en los descriptores. Pero tomando como referencia al gráfico y la pequeña diferencia que hay entre la media y la desviación, podemos aproximar esta variable con un modelo de distribución exponencial.

En esta parte mostraremos un ejemplo hipotético en el cual tomaremos como lambda a la inversa de la media
```{r}
lambda = 1/media
```

Ejemplo: Queremos saber cual es la probabilidad en caso si queremos introducir un nuevo juego al mercado, este tenga al menos 1 millón de ventas globales.

```{r}
# Graficamos 
muestra <- rexp(10000, rate = lambda)
hist(muestra, breaks = 70, prob = TRUE, col = "#008B8B",  xlab = "Ventas Globales(millones)", ylab = "f(x)",main="Distribucion Exponencial")

#Calculamos la probabilidad de que el juego introducido tenga al menos 1 millón de ventas 
pexp(1,lambda,lower.tail = FALSE)
```

Con este modelo podemos concluir que la probabilidad de introducir un nuevo juego y este tenga al menos 1 millón de ventas globales es 0.1401853.



## CONCLUCIONES:

1)En conclusión durante todo el proyecto para la parte de numero de ventas hemos podido analizar desde como puede cambiar el  de cuerdo al continente que se vende o país  la cual  varia, además de hacer un análisis si la venta puede depender de otra variable o es independiente, en este caso en probabilidades condicionales pudimos ver que si el tipo de plataforma influye en el número de ventas


2)Se analizó la variable plataforma para saber cuales fueron las más utilizadas entre los años 2015 y 2016 los resultados fueron:  Action y Shooter son los más destacados con esto se puede concluir que estás tendrán más probabilidad de tener éxito en su lanzamiento además por su preferencias por parte de usuarios y profesionales.


3)Por ultimo se vio el comportamiento de los videojuegos en funcion a sus ventas globales, lo cual se logro aproximar a un modelo continuo, por lo que nos sirvió para poder predecir y estimar la probabilidad en el caso queramos introducir un nuevo videojuego al mercado y este pueda pasar el numero de ventas que la empresa desea tener.  


